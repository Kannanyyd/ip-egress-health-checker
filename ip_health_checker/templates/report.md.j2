# 自有服务器出口 IP 健康检查报告

This project currently renders Markdown with the built-in stdlib renderer in
`ip_health_checker/report.py`. This template is reserved for users who prefer
to customize reporting with Jinja2.
